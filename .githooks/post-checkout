#!/bin/sh
# Keeps the Vault aligned when switching topic branches.
# Missing config/vault or a checkout without obsidian/ is intentionally a no-op.
repo_root="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0
python3 "$repo_root/scripts/sync_obsidian.py" || true
exit 0
